#ifndef MAXIMUM_VALUE_HPP
#define MAXIMUM_VALUE_HPP

#include <iostream>
#include <vector>

int compare_to_max(std::vector<int> input, int n);
int compare_to_all(std::vector<int> input, int n);

#endif // MAXIMUM_VALUE_HPP